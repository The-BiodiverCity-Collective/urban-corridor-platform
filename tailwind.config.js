/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './templates/**/*.html',   // Tailwind will scan all HTML files inside the templates folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

